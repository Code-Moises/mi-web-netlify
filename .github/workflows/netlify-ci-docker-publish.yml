name: CI - Build Docker, Push to GHCR and Update Netlify Site

on:
  push:
    branches:
      - main

permissions:
  contents: write
  packages: write

jobs:
  build-push-and-update-site:
    runs-on: ubuntu-latest
    env:
      # He cambiado 'mi-web-app' por 'mi-web-netlify' para que coincida con tu repo
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/mi-web-netlify
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up QEMU (optional multi-arch)
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image to GHCR
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Create build info
        id: buildinfo
        run: |
          echo "BUILD_TS=$(date -u +"%Y-%m-%dT%H:%M:%SZ")" >> $GITHUB_ENV
          echo "BUILD_SHA=${GITHUB_SHA}" >> $GITHUB_ENV
          echo "BUILD_TAG=${IMAGE_NAME}:${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Inject build info into index.html
        run: |
          SITE_INDEX=index.html
          # Si no existe, lo crea (aunque tú ya lo tienes)
          if [ ! -f "$SITE_INDEX" ]; then
            echo "<html><body><h1>Autogenerated site</h1></body></html>" > "$SITE_INDEX"
          fi
          
          # Preparamos el bloque HTML con los datos
          BUILD_BLOCK="<p><strong>Build:</strong> $BUILD_TS <br> <strong>SHA:</strong> $BUILD_SHA <br> <strong>Image:</strong> $BUILD_TAG</p>"
          
          # Busca tus etiquetas y reemplaza lo de dentro
          if grep -q "" "$SITE_INDEX"; then
            perl -0777 -pe "s/.*/\n$BUILD_BLOCK\n/s" -i "$SITE_INDEX"
          else
            # Fallback por si no encuentra las etiquetas
            if grep -q "</body>" "$SITE_INDEX"; then
              perl -0777 -pe "s|</body>|\n$BUILD_BLOCK\n\n</body>|s" -i "$SITE_INDEX"
            else
              echo -e "\n\n$BUILD_BLOCK\n" >> "$SITE_INDEX"
            fi
          fi

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "ci: update site with build info ${{ env.BUILD_TS }} (${{ env.BUILD_SHA }})"
          file_pattern: "index.html"
          branch: main
          author_name: github-actions[bot]
          author_email: 41898282+github-actions[bot]@users.noreply.github.com
